import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'amv-footer',
  imports: [RouterLink],
  templateUrl: './footer.html',
})
export class Footer {}
